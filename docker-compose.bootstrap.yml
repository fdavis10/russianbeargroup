services:
  nginx:
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.bootstrap.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certbot/www:/var/www/certbot

  certbot:
    image: certbot/certbot:latest
    profiles: ["certbot"]
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
